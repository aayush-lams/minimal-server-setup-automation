---
- name: Configure cron jobs only (scripts already exist)
  hosts: linux
  become: yes
  
  tasks:
    - name: Ensure scripts are executable
      file:
        path: "{{ item }}"
        mode: '0755'
      loop:
        - /usr/local/bin/backup.sh
        - /usr/local/bin/health.sh
    
    - name: Schedule daily backup
      cron:
        name: "Daily backup"
        minute: "0"
        hour: "2"
        job: "/usr/local/bin/backup.sh >> /var/log/backup.log 2>&1"
        user: "asur"
    
    - name: Schedule daily health check
      cron:
        name: "Daily health check"
        minute: "0"
        hour: "7"
        job: "/usr/local/bin/health.sh >> /var/log/health.log 2>&1"
        user: "asur"
    
    - name: Schedule reboot health check
      cron:
        name: "Reboot health check"
        special_time: "reboot"
        job: "sleep 60 && /usr/local/bin/health.sh >> /var/log/health.log 2>&1"
        user: "asur"
